@model IEnumerable<AplicacionWeb.Models.ComentariosCliente>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Comentarios)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Fecha_Comentario)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Programas.Nombre_Programa)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Usuarios.Nombre)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Comentarios)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Fecha_Comentario)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Programas.Nombre_Programa)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Usuarios.Nombre)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id_Comentario }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id_Comentario }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id_Comentario })
        </td>
    </tr>
}

</table>
